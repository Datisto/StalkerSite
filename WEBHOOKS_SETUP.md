# Налаштування Google Sheets та Discord Webhooks

## Огляд

Система тестування правил автоматично відправляє результати у Google Sheets та Discord канал адміністрації.

## 1. Google Sheets Integration

### Крок 1: Створення Google Apps Script

1. Відкрийте [Google Sheets](https://sheets.google.com)
2. Створіть нову таблицю з назвою "STALKER RP - Здача правил"
3. Перейдіть у **Extensions → Apps Script**
4. Видаліть існуючий код і вставте наступний:

```javascript
function doPost(e) {
  try {
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    const data = JSON.parse(e.postData.contents);

    // Якщо це перший запис, додайте заголовки
    if (sheet.getLastRow() === 0) {
      const headers = [
        'Дата/Час',
        'Steam ID',
        'Discord ID',
        'Питання 1',
        'Відповідь 1',
        'Питання 2',
        'Відповідь 2',
        'Питання 3',
        'Відповідь 3',
        'Питання 4',
        'Відповідь 4',
        'Питання 5',
        'Відповідь 5',
        'Питання 6',
        'Відповідь 6',
        'Питання 7',
        'Відповідь 7',
        'Питання 8',
        'Відповідь 8',
        'Питання 9',
        'Відповідь 9',
        'Питання 10',
        'Відповідь 10',
        'Статус перевірки'
      ];
      sheet.appendRow(headers);

      // Форматування заголовків
      const headerRange = sheet.getRange(1, 1, 1, headers.length);
      headerRange.setFontWeight('bold');
      headerRange.setBackground('#4a5568');
      headerRange.setFontColor('#ffffff');
    }

    // Підготовка рядка даних
    const row = [
      new Date(data.timestamp),
      data.steam_id,
      data.discord_id
    ];

    // Додаємо 10 пар питання-відповідь
    data.questions_and_answers.forEach(qa => {
      row.push(qa.question);
      row.push(qa.answer);
    });

    // Статус перевірки
    row.push('На розгляді');

    // Додаємо рядок
    sheet.appendRow(row);

    // Форматування нового рядка
    const lastRow = sheet.getLastRow();
    const range = sheet.getRange(lastRow, 1, 1, row.length);
    range.setWrap(true);

    // Виділяємо питання жирним
    for (let i = 4; i <= 22; i += 2) {
      sheet.getRange(lastRow, i).setFontWeight('bold');
    }

    // Автоматичне розширення колонок
    sheet.autoResizeColumns(1, 3);

    return ContentService
      .createTextOutput(JSON.stringify({ success: true }))
      .setMimeType(ContentService.MimeType.JSON);

  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({
        success: false,
        error: error.toString()
      }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}
```

### Крок 2: Публікація Apps Script

1. Натисніть **Deploy → New deployment**
2. Виберіть тип: **Web app**
3. Налаштування:
   - **Description:** STALKER RP Rules Test Webhook
   - **Execute as:** Me
   - **Who has access:** Anyone
4. Натисніть **Deploy**
5. Скопіюйте **Web app URL** (виглядає як: `https://script.google.com/macros/s/...../exec`)
6. Додайте цей URL в `.env` файл:

```bash
VITE_GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
```

### Крок 3: Тестування

1. Після налаштування пройдіть тест на сайті
2. Перевірте, чи з'явився запис у Google Sheets
3. Якщо помилка - перевірте логи в Apps Script (View → Executions)

### Формат таблиці

Кожен рядок містить:
- **Колонка A:** Дата і час здачі
- **Колонка B:** Steam ID користувача
- **Колонка C:** Discord ID користувача
- **Колонки D-U:** 10 пар питань та відповідей
- **Колонка V:** Статус перевірки (На розгляді / Зараховано / Не зараховано)

## 2. Discord Webhook

### Крок 1: Створення Webhook у Discord

1. Відкрийте налаштування вашого Discord сервера
2. Перейдіть у **Server Settings → Integrations → Webhooks**
3. Натисніть **New Webhook**
4. Налаштування:
   - **Name:** STALKER RP Rules Bot
   - **Channel:** Виберіть канал для сповіщень (наприклад, #admin-notifications)
   - Можете встановити аватар бота
5. Натисніть **Copy Webhook URL**
6. Додайте URL в `.env` файл:

```bash
VITE_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
```

### Формат повідомлення

Webhook відправляє embed повідомлення з:
- 📝 Заголовок: "Нова здача правил"
- Steam ID
- Discord ID
- Дата та час
- Червоний колір рамки

### Приклад повідомлення

```
📝 Нова здача правил

Steam ID: 76561198123456789
Discord ID: 123456789012345678
Дата: 26.11.2025, 23:45:30
```

### Крок 2: Тестування

1. Пройдіть тест на сайті
2. Перевірте Discord канал на наявність повідомлення
3. Якщо повідомлення не з'явилось - перевірте URL webhook

## 3. Структура .env файлу

Додайте наступні змінні у ваш `.env` файл:

```bash
# Supabase
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Sheets Webhook
VITE_GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# Discord Webhook
VITE_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
```

## 4. Як це працює

### Послідовність дій при здачі тесту:

1. **Гравець заповнює тест** (10 питань з письмовими відповідями)
2. **Натискає "Відправити"**
3. **Система виконує 3 дії паралельно:**
   - ✅ Зберігає в базу Supabase (таблиця `rules_test_submissions`)
   - ✅ Відправляє в Google Sheets (через Apps Script webhook)
   - ✅ Відправляє сповіщення в Discord (через Discord webhook)
4. **Гравець отримує підтвердження**
5. **Адміністрація:**
   - Отримує сповіщення в Discord
   - Бачить повні відповіді в Google Sheets
   - Може додати коментарі та змінити статус

## 5. Безпека

### Google Sheets:
- ✅ Таблиця доступна тільки власнику або тим, кому ви надасте доступ
- ✅ Apps Script виконується від вашого імені
- ✅ Webhook URL публічний, але не містить чутливих даних

### Discord:
- ✅ Webhook URL не потрібно захищати на рівні сервера
- ✅ Якщо URL скомпрометовано - просто створіть новий webhook
- ✅ Повідомлення містять тільки базову інформацію (Steam ID, Discord ID)

### Рекомендації:
- 🔒 Не коммітьте `.env` файл в git (вже в `.gitignore`)
- 🔒 Періодично оновлюйте webhook URLs
- 🔒 Обмежте доступ до Google Sheets тільки адміністрації
- 🔒 Використовуйте окремий Discord канал тільки для адмінів

## 6. Робота без webhooks (опціонально)

Якщо ви не налаштували webhooks, система продовжить працювати:
- ✅ Дані зберігаються в Supabase
- ✅ Адміністрація може переглядати здачі в адмін-панелі
- ⚠️ Але немає автоматичних сповіщень

## 7. Перегляд здач в адмін-панелі

Адміністратори можуть переглядати всі здачі:
1. Увійдіть в адмін-панель (`/admin`)
2. Перейдіть у вкладку "Банк питань"
3. Там буде секція "Здачі тесту" з усіма результатами

## 8. Troubleshooting

### Google Sheets не отримує дані:

1. Перевірте URL webhook в `.env`
2. Перевірте логи в Apps Script (View → Executions)
3. Переконайтеся, що script опублікований (Deploy → Manage deployments)
4. Перевірте права доступу (Execute as: Me, Who has access: Anyone)

### Discord не отримує повідомлення:

1. Перевірте URL webhook в `.env`
2. Переконайтеся, що webhook активний у налаштуваннях Discord
3. Перевірте права бота на відправку повідомлень у канал
4. Спробуйте створити новий webhook

### Дані не зберігаються в Supabase:

1. Перевірте RLS політики таблиці `rules_test_submissions`
2. Перевірте логи помилок у консолі браузера
3. Переконайтеся, що користувач авторизований

## 9. Корисні посилання

- [Google Apps Script Documentation](https://developers.google.com/apps-script)
- [Discord Webhooks Guide](https://support.discord.com/hc/en-us/articles/228383668-Intro-to-Webhooks)
- [Supabase RLS Documentation](https://supabase.com/docs/guides/auth/row-level-security)
